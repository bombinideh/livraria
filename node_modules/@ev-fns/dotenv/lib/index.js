"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.dotenv = void 0;
const dotenv_safe_1 = require("dotenv-safe");
const dotenv = ({ output = () => {
    if (process.env.NODE_ENV === "test") {
        return;
    }
    if (process.env.NODE_ENV) {
        console.info(`🌟 ${process.env.NODE_ENV}`);
    }
    if (process.env.VERSION || process.env.npm_package_version) {
        console.info(`🔖 ${process.env.VERSION || process.env.npm_package_version}`);
    }
}, ...props } = {}) => {
    dotenv_safe_1.config(props);
    if (output) {
        output();
    }
};
exports.dotenv = dotenv;
